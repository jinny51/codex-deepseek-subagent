interface:
  display_name: "Set Up DeepSeek Subagent"
  short_description: "Install, inspect, or remove the managed worker"
  default_prompt: "$setup-deepseek-subagent Inspect and install the managed DeepSeek evidence worker."

policy:
  allow_implicit_invocation: false
